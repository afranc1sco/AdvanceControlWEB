<p id="notice"><%= notice %></p>

<h1>Listing Catunidadmedida</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @catunidadmedida.each do |catunidadmed| %>
      <tr>
        <td><%= link_to 'Show', catunidadmed %></td>
        <td><%= link_to 'Edit', edit_catunidadmed_path(catunidadmed) %></td>
        <td><%= link_to 'Destroy', catunidadmed, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Catunidadmed', new_catunidadmed_path %>
