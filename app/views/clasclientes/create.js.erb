$("#mynewclascliente").modal('hide');
$(".clascliente_descripcion").val('');


$("#container_clasclientes").prepend('<%= j render @clascliente %>'); //Prepend before append
$("#clascliente_<%= @clascliente.id %>").hide().fadeIn(1000);
$("#notice").html("<%= escape_javascript(render :partial => 'partials/flash' , :locals => { :flash => flash }).html_safe %>");

setTimeout(function(){
  $('#notice').fadeIn("slow", function() {
    $(this).create();
  })
}, 1500);
