$("#myupdatecomision_<%= @comision.id %>").modal('hide');

$("#comision_<%= @comision.id %>").fadeOut(500, function(){
  $(this).remove();
  $(".child").remove();
  $("#container_th_comision").prepend('<%= j render @comision %>');

});
$("#notice").html("<%= escape_javascript(render :partial => 'partials/flash' , :locals => { :flash => flash }).html_safe %>");

setTimeout(function(){
  $('#notice').fadeIn("slow", function() {
    $(this).create();
  })
}, 1500);
